<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/restaurant_detail_title_root"
                xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="42dp"
                android:background="@color/window_background_light"
                android:foreground="?attr/selectableItemBackground">

    <TextView
        android:id="@+id/restaurant_detail_title_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="16dp"
        android:layout_marginStart="16dp"
        android:text="인기메뉴"
        android:textSize="14sp"/>

    <ImageView
        android:id="@+id/restaurant_detail_title_arrow"
        android:layout_width="42dp"
        android:layout_height="42dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:padding="15dp"
        android:scaleType="fitCenter"
        android:src="@mipmap/btn_arrow_bottom"/>

</RelativeLayout>